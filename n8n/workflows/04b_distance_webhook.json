{
  "id": "wf04b-distance-realtime-v1",
  "name": "04B - Bronze | Distance App → MinIO (Webhook Temps Réel)",
  "active": true,
  "nodes": [
    {
      "id": "webhook-dist-001",
      "name": "Webhook Distance Update",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        100,
        300
      ],
      "webhookId": "distance-update-realtime",
      "parameters": {
        "httpMethod": "POST",
        "path": "distance-update",
        "responseMode": "responseNode",
        "options": {}
      }
    },
    {
      "id": "code-auth-001",
      "name": "Auth — Valider X-API-KEY",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        360,
        300
      ],
      "parameters": {
        "jsCode": "const data    = $input.first().json;\nconst headers = data.headers || {};\n\n// Accepter les deux casses courantes du header\nconst provided = (headers['x-api-key'] || headers['X-API-KEY'] || '').trim();\nconst expected = ($env.GPS_WEBHOOK_SECRET || '').trim();\n\nif (!expected) {\n  // Variable non configurée → refus par sécurité\n  console.warn('[WF-04B] GPS_WEBHOOK_SECRET non défini dans les variables n8n');\n  return [{ json: { _authorized: false, _reason: 'server_misconfiguration' } }];\n}\n\nconst authorized = provided !== '' && provided === expected;\nif (!authorized) {\n  console.warn(`[WF-04B] ❌ Clé API invalide — rejet (provided length: ${provided.length})`);\n}\n\n// Passer tout le payload original en conservant le flag d'autorisation\nreturn [{ json: { ...data, _authorized: authorized } }];"
      }
    },
    {
      "id": "if-auth-001",
      "name": "Autorisé ?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        600,
        300
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "cond-auth-001",
              "leftValue": "={{ $json._authorized }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true"
              }
            }
          ],
          "combinator": "and"
        }
      }
    },
    {
      "id": "respond-401-001",
      "name": "Répondre 401 Unauthorized",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        840,
        460
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "{\"error\":\"Unauthorized\",\"message\":\"Missing or invalid X-API-KEY header\"}",
        "options": {
          "responseCode": 401
        }
      }
    },
    {
      "id": "code-accumulate-001",
      "name": "Accumuler Session",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        840,
        180
      ],
      "parameters": {
        "jsCode": "const fs     = require('fs');\nconst crypto = require('crypto');\n\nconst raw   = $input.first().json;\n// Body is in raw.body when POST with JSON; for GET/empty body, fall back to raw itself\nconst data  = raw.body || raw;\n// Merge query params (works for both GET ?vehicle_id=VH-001 and POST body)\nconst query = raw.query || {};\n\n// Reconstruct event from body OR query params\nconst event = {};\nfor (const [k,v] of Object.entries({ ...data })) {\n  if (!k.startsWith('_')) event[k] = v;\n}\n// Fill missing required fields from query params or smart defaults\nif (!event.event)      event.event      = query.event      || 'distance_update';\nif (!event.session_id) event.session_id = query.session_id || null;\nif (!event.vehicle_id) event.vehicle_id = query.vehicle_id || query.vehicleId || null;\n\nfunction localDateStr() {\n  return new Date().toLocaleDateString('sv-SE', { timeZone: 'Europe/Paris' });\n}\nfunction localDateFrom(iso) {\n  return new Date(iso).toLocaleDateString('sv-SE', { timeZone: 'Europe/Paris' });\n}\n\n// Auto-generate daily session_id: VH-001-2026-06-24\nif (!event.session_id && event.vehicle_id) {\n  const today = localDateStr();\n  event.session_id = `${event.vehicle_id}-${today}`;\n}\n\nif (!event.session_id || !['distance_update', 'session_end'].includes(event.event)) {\n  console.warn(`[WF-04B] Payload invalide: ${JSON.stringify(event).substring(0,200)}`);\n  return [{ json: { _payloadError: 'Payload invalide — champs requis: session_id (ou vehicle_id pour auto-génération) et event dans {distance_update,session_end}' } }];\n}\n\nconst STAGING_FILE   = '/staging/distances/distances_realtime.json';\nconst CHECKSUM_FILE  = '/staging/_metadata/checksums/distances_realtime.json';\nconst MINIO_PREFIX   = 'etl-projet-bc/raw/distances';\nconst MINIO_FILENAME = 'distances_realtime.json';\n\nlet accumulated = { metadata: { generated_at: new Date().toISOString(), count: 0 }, data: [] };\ntry {\n  const existing = fs.readFileSync(STAGING_FILE, 'utf8');\n  accumulated = JSON.parse(existing);\n  if (!Array.isArray(accumulated.data)) accumulated.data = [];\n} catch(e) {}\n\nconst sessionId = String(event.session_id);\nconst vehicleId = event.vehicle_id || event.vehicleId\n               || (raw.query && (raw.query.vehicle_id || raw.query.vehicleId))\n               || 'VH-UNKNOWN';\n\nlet session = accumulated.data.find(s => s.session_id === sessionId);\n\nif (event.event === 'session_end') {\n  const pts    = Array.isArray(event.location_points) ? event.location_points : [];\n  const lastPt = pts.length > 0 ? pts[pts.length - 1] : null;\n  if (!session) { session = { session_id: sessionId }; accumulated.data.push(session); }\n  session.session_date          = event.start_time ? localDateFrom(event.start_time) : localDateStr();\n  session.vehicle_id            = session.vehicle_id || vehicleId;\n  session.total_distance_km     = parseFloat((event.total_distance_km     || 0).toFixed(6));\n  session.total_distance_meters = parseFloat((event.total_distance_meters || 0).toFixed(2));\n  session.max_speed_kmh         = parseFloat((event.max_speed_kmh         || 0).toFixed(3));\n  session.avg_speed_kmh         = parseFloat((event.avg_speed_kmh         || 0).toFixed(3));\n  session.total_active_seconds  = parseInt(event.active_seconds || 0);\n  session.event_count           = (session.event_count || 0) + 1;\n  session.first_event_at        = event.start_time || session.first_event_at || null;\n  session.last_event_at         = event.end_time   || session.last_event_at  || null;\n  session.last_latitude         = lastPt ? (lastPt.latitude  || lastPt.lat  || null) : (session.last_latitude  || null);\n  session.last_longitude        = lastPt ? (lastPt.longitude || lastPt.lon  || null) : (session.last_longitude || null);\n  console.log(`[WF-04B] session_end ${sessionId}: ${session.total_distance_km.toFixed(4)} km | avg ${session.avg_speed_kmh} km/h | ${session.total_active_seconds}s`);\n} else {\n  // distance_update : accumulation incrémentale\n  const loc = event.location || {};\n  // Support lat/lon/speed at top level (from phone query params or flat body)\n  const lat = loc.latitude  || event.lat || event.latitude  || null;\n  const lon = loc.longitude || event.lon || event.longitude || null;\n  const spd = parseFloat(event.speed_kmh || event.speed || event.kmh || 0);\n  if (!session) {\n    session = {\n      session_id            : sessionId,\n      session_date          : event.timestamp ? localDateFrom(event.timestamp) : localDateStr(),\n      vehicle_id            : vehicleId,\n      total_distance_km     : 0,\n      total_distance_meters : 0,\n      max_speed_kmh         : 0,\n      avg_speed_kmh         : 0,\n      total_active_seconds  : 0,\n      event_count           : 0,\n      first_event_at        : event.timestamp || new Date().toISOString(),\n      last_event_at         : event.timestamp || new Date().toISOString(),\n      last_latitude         : lat,\n      last_longitude        : lon\n    };\n    accumulated.data.push(session);\n  }\n  if (vehicleId && !session.vehicle_id) session.vehicle_id = vehicleId;\n  session.total_distance_km      = parseFloat((session.total_distance_km + (event.distance_km || 0)).toFixed(6));\n  session.total_distance_meters  = parseFloat((session.total_distance_meters + (event.distance_meters || 0)).toFixed(2));\n  if (spd > session.max_speed_kmh) session.max_speed_kmh = parseFloat(spd.toFixed(3));\n  session.total_active_seconds  += (event.active_seconds || 0);\n  session.event_count           += 1;\n  if (session.total_active_seconds > 0)\n    session.avg_speed_kmh = parseFloat(((session.total_distance_km / session.total_active_seconds) * 3600).toFixed(3));\n  const ts = event.timestamp || new Date().toISOString();\n  if (!session.first_event_at || ts < session.first_event_at) session.first_event_at = ts;\n  if (!session.last_event_at  || ts > session.last_event_at)  session.last_event_at  = ts;\n  if (lat)  session.last_latitude  = lat;\n  if (lon)  session.last_longitude = lon;\n  console.log(`[WF-04B] distance_update ${sessionId}: evt#${session.event_count} | ${session.total_distance_km.toFixed(4)} km | véhicule: ${session.vehicle_id || 'N/A'}`);\n}\n\naccumulated.metadata.generated_at = new Date().toISOString();\naccumulated.metadata.count        = accumulated.data.length;\n\nconst payload     = JSON.stringify(accumulated);\nconst buf         = Buffer.from(payload, 'utf8');\nconst currentHash = crypto.createHash('md5').update(buf).digest('hex');\n\nlet storedHash = null;\ntry {\n  const chk = JSON.parse(fs.readFileSync(CHECKSUM_FILE, 'utf8'));\n  storedHash = chk.hash;\n} catch(e) {}\n\nconst localDate = localDateStr();\nconst [y, m, d] = localDate.split('-');\nconst pParts = new Intl.DateTimeFormat('fr-FR', { timeZone: 'Europe/Paris', hour: '2-digit', minute: '2-digit', hour12: false }).formatToParts(new Date());\nconst hh = pParts.find(p => p.type === 'hour').value.padStart(2,'0');\nconst mm = pParts.find(p => p.type === 'minute').value.padStart(2,'0');\nconst ts  = `${y}${m}${d}_${hh}${mm}`;\n\nconst versionedKey = `${MINIO_PREFIX}/${y}/${m}/${d}/${ts}/${MINIO_FILENAME}`;\nconst latestKey    = `${MINIO_PREFIX}/latest/${MINIO_FILENAME}`;\n\nconst binAttach = { data: buf.toString('base64'), mimeType: 'application/json', fileName: MINIO_FILENAME };\n\nreturn [{\n  json: {\n    currentHash, storedHash,\n    hashChanged   : currentHash !== storedHash,\n    versionedKey, latestKey,\n    timestamp     : new Date().toISOString(),\n    count         : accumulated.data.length,\n    sessionId,\n    vehicleId     : session.vehicle_id,\n    eventCount    : session.event_count,\n    lastLatitude  : session.last_latitude,\n    lastLongitude : session.last_longitude\n  },\n  binary: { distData: binAttach }\n}];"
      }
    },
    {
      "id": "if-changed-001",
      "name": "Données modifiées ?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        1100,
        180
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "cond-hash-001",
              "leftValue": "={{ $json.hashChanged }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true"
              }
            }
          ],
          "combinator": "and"
        }
      }
    },
    {
      "id": "s3-versioned-001",
      "name": "MinIO Versionné — distances",
      "type": "n8n-nodes-base.s3",
      "typeVersion": 1,
      "position": [
        1360,
        60
      ],
      "credentials": {
        "s3": {
          "id": "Iqpe5A6QfeoXeIZR",
          "name": "MinIO S3 (path-style)"
        }
      },
      "parameters": {
        "operation": "upload",
        "bucketName": "bronze",
        "fileName": "={{ $json.versionedKey }}",
        "binaryData": true,
        "binaryPropertyName": "distData",
        "additionalFields": {
          "acl": "private"
        }
      }
    },
    {
      "id": "code-restore-001",
      "name": "Restaurer distData",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1580,
        60
      ],
      "parameters": {
        "jsCode": "const cdcOut = $('Accumuler Session').first();\nconst s3Out  = $input.first();\nif (!cdcOut.binary || !cdcOut.binary.distData) {\n  throw new Error('[WF-04B] Binaire distData introuvable dans Accumuler Session');\n}\nreturn [{ json: { ...cdcOut.json, ...s3Out.json }, binary: cdcOut.binary }];"
      }
    },
    {
      "id": "s3-latest-001",
      "name": "MinIO latest — distances",
      "type": "n8n-nodes-base.s3",
      "typeVersion": 1,
      "position": [
        1800,
        60
      ],
      "credentials": {
        "s3": {
          "id": "Iqpe5A6QfeoXeIZR",
          "name": "MinIO S3 (path-style)"
        }
      },
      "parameters": {
        "operation": "upload",
        "bucketName": "bronze",
        "fileName": "={{ $json.latestKey }}",
        "binaryData": true,
        "binaryPropertyName": "distData",
        "additionalFields": {
          "acl": "private"
        }
      }
    },
    {
      "id": "code-stage-001",
      "name": "Stage+Checksum — distances",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2020,
        60
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\n\nconst cdcOut = $('Accumuler Session').first();\nif (!cdcOut.binary || !cdcOut.binary.distData) {\n  throw new Error('[WF-04B] Binaire distData introuvable');\n}\nconst buf = Buffer.from(cdcOut.binary.distData.data, 'base64');\n\nfs.mkdirSync('/staging/distances', { recursive: true });\nfs.writeFileSync('/staging/distances/distances_realtime.json', buf);\n\nconst dir = '/staging/_metadata/checksums';\nfs.mkdirSync(dir, { recursive: true });\nfs.writeFileSync(`${dir}/distances_realtime.json`,\n  JSON.stringify({\n    hash        : cdcOut.json.currentHash,\n    updatedAt   : new Date().toISOString(),\n    count       : cdcOut.json.count,\n    versionedKey: cdcOut.json.versionedKey\n  }, null, 2)\n);\n\nconsole.log(`[WF-04B] distances_realtime.json stagé (${cdcOut.json.count} sessions) → ${cdcOut.json.versionedKey}`);\nreturn [{ json: {\n  status        : 'success',\n  count         : cdcOut.json.count,\n  versionedKey  : cdcOut.json.versionedKey,\n  sessionId     : cdcOut.json.sessionId,\n  vehicleId     : cdcOut.json.vehicleId,\n  lastLatitude  : cdcOut.json.lastLatitude,\n  lastLongitude : cdcOut.json.lastLongitude\n} }];"
      }
    },
    {
      "id": "code-skip-001",
      "name": "Skip — inchangé",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1360,
        300
      ],
      "parameters": {
        "jsCode": "const item = $input.first().json;\nconsole.log(`[WF-04B] ⏭️ Session ${item.sessionId} — données inchangées, pas d'upload`);\nreturn [{ json: { status: 'skipped', sessionId: item.sessionId } }];"
      }
    },
    {
      "id": "respond-ok-001",
      "name": "Répondre 200 OK",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        2240,
        60
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ status: 'ok', message: 'Distance event processed', sessionId: $json.sessionId || $('Accumuler Session').first().json.sessionId }) }}",
        "options": {
          "responseCode": 200
        }
      }
    },
    {
      "id": "exec-wf08d-from-04b-001",
      "name": "Déclencher WF-08D → Distances BC",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1,
      "position": [
        2480,
        60
      ],
      "parameters": {
        "workflowId": "wf08d-sync-distances-v1",
        "waitForSubWorkflow": true,
        "options": {},
        "source": "database"
      }
    },
    {
      "id": "respond-skip-001",
      "name": "Répondre 200 Skip",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1580,
        300
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ status: 'ok', message: 'No change detected', sessionId: $json.sessionId }) }}",
        "options": {
          "responseCode": 200
        }
      }
    },
    {
      "id": "if-ping-001",
      "name": "Ping ?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        720,
        180
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": false,
            "leftValue": "",
            "typeValidation": "loose"
          },
          "conditions": [
            {
              "id": "cond-ping-001",
              "leftValue": "={{ ($json.body || $json).event }}",
              "rightValue": "ping",
              "operator": {
                "type": "string",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        }
      }
    },
    {
      "id": "respond-pong-001",
      "name": "Répondre 200 Pong",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        960,
        60
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "{\"status\":\"ok\",\"message\":\"pong\",\"server\":\"n8n-etl-projet-bc\"}",
        "options": {
          "responseCode": 200
        }
      }
    },
    {
      "id": "if-payload-001",
      "name": "Payload valide ?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        980,
        180
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose"
          },
          "conditions": [
            {
              "id": "cond-payload-001",
              "leftValue": "={{ $json._payloadError }}",
              "rightValue": "",
              "operator": {
                "type": "string",
                "operation": "empty"
              }
            }
          ],
          "combinator": "and"
        }
      }
    },
    {
      "id": "respond-400-001",
      "name": "Répondre 400 Bad Request",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1180,
        340
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ error: 'Bad Request', message: $json._payloadError }) }}",
        "options": {
          "responseCode": 400
        }
      }
    }
  ],
  "connections": {
    "Webhook Distance Update": {
      "main": [
        [
          {
            "node": "Auth — Valider X-API-KEY",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Auth — Valider X-API-KEY": {
      "main": [
        [
          {
            "node": "Autorisé ?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Autorisé ?": {
      "main": [
        [
          {
            "node": "Ping ?",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Répondre 401 Unauthorized",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Ping ?": {
      "main": [
        [
          {
            "node": "Répondre 200 Pong",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Accumuler Session",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Accumuler Session": {
      "main": [
        [
          {
            "node": "Payload valide ?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Données modifiées ?": {
      "main": [
        [
          {
            "node": "MinIO Versionné — distances",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Skip — inchangé",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "MinIO Versionné — distances": {
      "main": [
        [
          {
            "node": "Restaurer distData",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Restaurer distData": {
      "main": [
        [
          {
            "node": "MinIO latest — distances",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "MinIO latest — distances": {
      "main": [
        [
          {
            "node": "Stage+Checksum — distances",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Stage+Checksum — distances": {
      "main": [
        [
          {
            "node": "Répondre 200 OK",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Répondre 200 OK": {
      "main": [
        [
          {
            "node": "Déclencher WF-08D → Distances BC",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Skip — inchangé": {
      "main": [
        [
          {
            "node": "Répondre 200 Skip",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Payload valide ?": {
      "main": [
        [
          {
            "node": "Données modifiées ?",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Répondre 400 Bad Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "meta": null,
  "pinData": {},
  "versionId": "eb48037b-653a-482a-8ab4-92a06e803443",
  "activeVersionId": "eb48037b-653a-482a-8ab4-92a06e803443",
  "versionCounter": 76,
  "triggerCount": 1,
  "tags": [
    {
      "name": "distances"
    },
    {
      "name": "realtime"
    },
    {
      "name": "secured"
    }
  ]
}